import { CommonArrayFilterPipe } from './common-array-filter.pipe';

describe('CommonArrayFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CommonArrayFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
